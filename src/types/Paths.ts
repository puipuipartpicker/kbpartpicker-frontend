enum Paths {
  main = '/' ,
  switch = '/switch',
  cases = '/cases',
  pcb = '/pcb',
  plates = '/plates',
  stabilizers = '/stabilizers',
  keycaps = '/keycaps',
  product = '/:productid'
}

export default Paths